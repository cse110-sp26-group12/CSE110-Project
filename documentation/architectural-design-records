# Architectural Design Record

Functional web app that runs on a server, the app should allow for communication between members and serve as a dashboard/hub for the team.
We'll use Render to host our web app as we develop. Security considerations will come later. We'll use markdown, HTML, CSS, and JavaScript
to develop our web app.
